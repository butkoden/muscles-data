# Changelog

## 0.1.0

- named resources, typed ports, lazy adapters и safe diagnostics используют versioned core dependency;
- common adapter registration contract остаётся независимым от vendor clients.
